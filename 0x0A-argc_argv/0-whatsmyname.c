#include "main.h"

/**
 * main - entry point
 * @argc: number of arguments
 * @argv: the arguments value
 * Return: 0
*/

int main(int argc __attribute__((unused)), char **argv)
{
	printf("%s\n", argv[0]);
	return (0);
}
