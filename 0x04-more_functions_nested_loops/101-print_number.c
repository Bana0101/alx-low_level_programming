#include "main.h"

/**
*/

void print_number(int n)
{
	int t, i;
	unsigned int cnt = 1;

	t = n;
	while ((t / 10) != 0 && t > 9)
	{
		t = t / 10;
		ctn++;
	}
	for (i = 1; i <= cnt; i++)
		_putchar((n / 10
}
